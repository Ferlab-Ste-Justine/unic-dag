-- noinspection SqlNoDataSourceInspectionForFile

-- create scores table
CREATE TABLE IF NOT EXISTS indicateurs_sip.scores (
    encounterId Int,
    dilirium String,
    scoreYear Int,
    scoreMonth Int
    );
